# Database Configuration
# For local development, copy this file to .env and fill in your values
# For production (cPanel), configure these in the hosting environment

MYSQL_HOST=your_database_host
MYSQL_DATABASE=your_database_name
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_database_password

# For cPanel deployment
# These values should be configured in cPanel's environment variables
# NOT stored in version control
