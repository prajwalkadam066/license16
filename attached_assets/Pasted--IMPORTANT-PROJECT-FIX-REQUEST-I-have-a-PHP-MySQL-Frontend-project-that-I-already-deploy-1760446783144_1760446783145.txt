🚨 IMPORTANT PROJECT FIX REQUEST 🚨

I have a PHP + MySQL + Frontend project that I already deployed to cPanel, but it is not working correctly.  
I’m getting this error in the browser console:
❌ Unexpected token '<', "<!DOCTYPE ..." is not valid JSON  
❌ Failed to load resource: the server responded with a status of 404 (Not Found)  
❌ Fetch error: SyntaxError: Unexpected token '<'  

Now I want you to carefully FIX EVERYTHING and make this project FULLY WORKING in Replit before I re-upload to cPanel.

Here are the required tasks 👇

---

### 🧠 PROJECT DETAILS

- **Backend Language:** PHP  
- **Database:** MySQL  
- **Frontend:** HTML / CSS / JavaScript (no React)  
- **API Folder:** `/api/`
- **Main API File:** `licenses.php`
- **Database Config File:** `db_config.php`
- **Frontend Path:** `/License/` (contains `index.html` and JS files)
- **Error:** Frontend fetches JSON data from `/api/licenses`, but gets HTML instead.

---

### ✅ WHAT YOU NEED TO DO

1. **Fix all backend issues:**
   - Make sure every PHP file in `/api/` returns **valid JSON**.
   - Add proper **CORS headers**, **Content-Type: application/json**, and **error handling**.
   - Handle database connection using PDO and show JSON errors only (no HTML).
   - Ensure correct relative paths (e.g., `require_once 'db_config.php';`).

2. **Fix frontend API calls:**
   - Change all fetch URLs from `License/api/licenses` → `/api/licenses.php` or `https://<mydomain>/api/licenses.php`.
   - Add error handling in JS to show API error messages clearly.

3. **Fix MySQL connection:**
   - Use correct credentials for `db_config.php`:
     ```php
     $host = 'localhost';
     $dbName = 'your_database_name';
     $dbUser = 'your_database_user';
     $dbPass = 'your_database_password';
     ```
   - Connect using PDO and handle connection errors properly.

4. **Return clean JSON from backend:**
   Example for `api/licenses.php`:
   ```php
   <?php
   header('Content-Type: application/json');
   header('Access-Control-Allow-Origin: *');
   header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS');
   header('Access-Control-Allow-Headers: Content-Type');

   require_once 'db_config.php';
   try {
       $stmt = $pdo->query("SELECT * FROM licenses");
       $data = $stmt->fetchAll(PDO::FETCH_ASSOC);
       echo json_encode($data);
   } catch (Exception $e) {
       echo json_encode(['error' => $e->getMessage()]);
   }
   ?>
